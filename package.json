{
  "name": "gacha4good",
  "version": "0.1.4",
  "description": "Players collect streamers to fight for them against the evil twitch emotes",
  "main": "game.js",
  "scripts": {
    "start": "budo game.js --live --open --port 8000 --watch-glob **/*.{html,js,css}",
    "copy-files": "rm -rf ./build && mkdir build && cp index.html ./build && cp -r ./plugins ./build && cp -r ./images ./build",
    "bundle": "browserify --entry game.js --outfile ./build/game.js",
    "build": "npm run copy-files && npm run bundle",
    "deploy": "npm run build && surge ./build gacha.surge.sh"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/troy-lamerton/gacha4good.git"
  },
  "author": "troy-lamerton",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/troy-lamerton/gacha4good/issues"
  },
  "homepage": "https://github.com/troy-lamerton/gacha4good#readme",
  "devDependencies": {
    "browserify": "^14.1.0"
  }
}
